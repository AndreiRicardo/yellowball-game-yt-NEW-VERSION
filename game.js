export const estados = {
    jogar: 0,
    jogando: 1,
    perdeu: 2
};

export function clique(event) {
    // Lógica do clique do mouse
}

export function passarDeFase() {
    // Lógica para passar de fase
}
